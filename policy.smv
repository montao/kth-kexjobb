
MODULE main
VAR
	user : {restricted, root};
	state : {nonsecure, secure};
ASSIGN
	init (state) := nonsecure;
	next (state) :=
		case
			state = nonsecure & (user = restricted) : secure;
			TRUE : {nonsecure, secure};
		esac;
SPEC AG ((user = restricted) -> AF state = secure)